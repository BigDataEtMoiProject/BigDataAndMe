<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.uqac.bigdataetmoi">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.SignupActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".activity.SommeilActivity" />
        <activity android:name=".activity.CompteurDePasActivity" />
        <activity android:name=".activity.TempsUtilisationActivity" />
        <activity android:name=".activity.TelephoneSmsActivity" />
        <activity android:name=".activity.DonneesUtilisationActivity" />
        <activity android:name=".activity.QuizzActivity"/>
        <activity android:name=".activity.QuizzPersonaliteActivity"/>
        <activity android:name=".activity.QuizzFacileActivity"/>
        <activity android:name=".activity.QuizzMoyenActivity"/>
        <activity android:name=".activity.QuizzDifficileActivity"/>
        <activity
            android:name=".activity.GPSMapsActivity"
            android:label="@string/title_activity_gpsmaps"/>

        <service
            android:name=".service.BigDataService"
            android:enabled="true"
            android:exported="false"/>

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
    </application>

</manifest>
